import React, { FC } from 'react'

interface Props {
}

const Navbar: FC<Props> = (Props) => {

    return (
        <div>
            This is a navbar that hasn't been implemented
        </div>
    );
}

export default Navbar;